name: RaycastedEntityOcclusions
version: ${version}
main: games.cubi.raycastedEntityOcclusion.RaycastedEntityOcclusion
api-version: '1.21'
prefix: REO
authors: [ _Cubicake ]
load: STARTUP
website: https://cubi.games
permissions:
    raycastedentityocclusions.command:
        description: Permission to use the RaycastedEntityOcclusions command
        default: op
    raycastedentityocclusions.bypass:
        description: Permission to bypass all checks
        default: false
    raycastedentityocclusions.updatecheck:
        description: Permission to check for plugin updates
        default: op
    raycastedentityocclusions.receivelogmessages:
        description: Permission to receive log messages which are sent to the console
        default: false
softdepend:
    - packetevents